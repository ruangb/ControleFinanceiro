@model IEnumerable<ControleFinanceiro.WebSite.Models.ExpenseInstallmentViewModel>

<table class="table table-striped table-hover table-condensed table-bordered">
    <thead>
        <tr>
            <th>
                &nbsp;
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdExpense)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense.OperationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Installment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @PersonViewModel.ObjectName
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    &nbsp;
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdExpense)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Expense.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Expense.OperationDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InstallmentOf)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Value)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Expense.Person.Name)
                </td>
                <td style="text-align: end">
                    @Html.ActionLink("Editar", "Edit", "Expense", new { id = item.IdExpense, installment = item.Installment }, new { @class = "btn btn-warning btn-sm" })
                    @Html.ActionLink("Detalhes", "Details", "Expense", new { id = item.IdExpense, installment = item.Installment }, new { @class = "btn btn-primary btn-sm" })
                    @Html.ActionLink("Excluir", "Delete", "Expense", new { id = item.IdExpense, installment = item.Installment }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }
    </tbody>
</table>
